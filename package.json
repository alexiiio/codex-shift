{
  "name": "codex-shift",
  "version": "0.2.0",
  "description": "Switch OpenAI Codex CLI accounts and start weekly usage windows with minimal quota.",
  "homepage": "https://github.com/alexiiio/codex-shift#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexiiio/codex-shift.git"
  },
  "bugs": {
    "url": "https://github.com/alexiiio/codex-shift/issues"
  },
  "type": "module",
  "bin": {
    "codex-shift": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run check && npm run build",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "keywords": ["openai", "codex", "cli", "account", "profile", "switcher"],
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
